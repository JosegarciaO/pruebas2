.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => CertificateRequestCoverage.model_name.human), "#/new"

.row
  .span12
    %table#certificate_request_coverages_table.table.table-striped
      %thead
        %tr
          %th= CertificateRequestCoverage.human_attribute_name(:certificate_request)
          %th= CertificateRequestCoverage.human_attribute_name(:coverage)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_certificate_request_coverages", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
