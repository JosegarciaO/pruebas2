.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => InsuranceCoverage.model_name.human), "#/new"

.row
  .span12
    %table#insurance_coverages_table.table.table-striped
      %thead
        %tr
          %th= InsuranceCoverage.human_attribute_name(:insurance)
          %th= InsuranceCoverage.human_attribute_name(:coverage)
          %th= InsuranceCoverage.human_attribute_name(:deductible_min)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_insurance_coverages", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
