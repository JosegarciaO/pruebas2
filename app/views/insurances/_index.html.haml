.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => Insurance.model_name.human), "#/new"

.row
  .span12
    %table#insurances_table.table.table-striped
      %thead
        %tr
          %th= Insurance.human_attribute_name(:name)
          %th= "Gastos de Expedición"
          %th= Insurance.human_attribute_name(:max_amount)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_insurances", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
