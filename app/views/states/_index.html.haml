.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => State.model_name.human), "#/new"

.row
  .span12
    %table#states_table.table.table-striped
      %thead
        %tr
          %th= State.human_attribute_name(:name)
          %th= State.human_attribute_name(:country_id)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_states", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
