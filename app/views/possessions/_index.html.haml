.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => Possession.model_name.human), "#/new"

.row
  .span12
    %table#possessions_table.table.table-striped
      %thead
        %tr
          %th= Possession.human_attribute_name(:name)
          %th= Possession.human_attribute_name(:has_max_amount)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_possessions", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
