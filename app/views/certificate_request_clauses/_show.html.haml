.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li= link_to t("links.back"), "#/index"
        %li= link_to t("links.edit"), "#/${id}/edit"
        %li= link_to t("links.destroy"), "#/index", :class => "destroy", :confirm => t("links.confirm")

.row
  .span8
    %p
      %strong= CertificateRequestClause.human_attribute_name(:certificate_request)
      ${certificate_request}

    %p
      %strong= CertificateRequestClause.human_attribute_name(:clause)
      ${clause}

    %p
      %strong= CertificateRequestClause.human_attribute_name(:is_included)
      ${is_included}


  .span4
    .dashboard
      .page-header
        %h3
      %p



