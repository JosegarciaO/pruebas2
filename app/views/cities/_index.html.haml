.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => City.model_name.human), "#/new"

.row
  .span12
    = text_field_tag "search_text", '', { :placeholder => 'Buscar ciudad ó estado', :class => 'input-large' }

.row
  .span12
    %table#cities_table.table.table-striped
      %thead
        %tr
          %th= City.human_attribute_name(:name)
          %th= City.human_attribute_name(:state_id)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_cities", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
