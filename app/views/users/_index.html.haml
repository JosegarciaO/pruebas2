.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => User.model_name.human), "#/new"

.row
  .span12
    %table#users_table.table.table-striped
      %thead
        %tr
          %th= User.human_attribute_name(:email)
          %th= User.human_attribute_name(:role_id)
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_users", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container
